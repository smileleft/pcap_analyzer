# pcap_analyzer (wit Python/FastAPI)

## How to start

```bash
# assume already installed Python 3.x and pip
# install tshark
apt install tshark

# install requirements
pip install -r requirements.txt

# run
python3 main.py
```
